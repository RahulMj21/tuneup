export const runtime = "edge";

const EventsPage = () => {
    return (
        <section title="events" className="px-4 py-6">
            <h1>Events</h1>
        </section>
    );
};

export default EventsPage;
