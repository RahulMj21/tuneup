export const runtime = "edge";

const DashboardPage = () => {
    return (
        <section title="dashboard">
            <h1>Dashboard</h1>
        </section>
    );
};

export default DashboardPage;
