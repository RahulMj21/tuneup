export const runtime = "edge";

const DashboardPage = () => {
    return (
        <section title="dashboard" className="px-4 py-6">
            <h1>Dashboard</h1>
        </section>
    );
};

export default DashboardPage;
