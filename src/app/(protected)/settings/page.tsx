export const runtime = "edge";

const SettingsPage = () => {
    return (
        <section title="settings" className="px-4 py-6">
            <h1>Settings</h1>
        </section>
    );
};

export default SettingsPage;
